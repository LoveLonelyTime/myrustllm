use myrustllm::autograd::impls::Autograd;
use myrustllm::common::dtype::{Any, F32, F64, I32};
use myrustllm::cpu::impls::CPU;

fn main() {
    // let t1 = Tensor::<CPU>::alloc(&Shape::scalar(), &());
    // let t2 = Tensor::<CPU,F64>::alloc(&Shape::scalar(), &());

    // let k = &t1 + &t2;
}
